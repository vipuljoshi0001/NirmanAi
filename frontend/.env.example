

# LLM (OpenRouter) — used by the FastAPI backend via the /api/llm/explain endpoint.
# Set OPENROUTER_API_KEY in the backend env (root .env) — the frontend only
# proxies /api calls to the backend.
OPENROUTER_API_KEY=""
APP_URL="http://localhost:5173"


